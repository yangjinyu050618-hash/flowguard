[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "flowguard"
version = "0.3.0"
description = "Lightweight, zero-dependency async resilience engine and LLM reliability gateway for Python."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [
    { name = "yangjinyu050618-hash", email = "2438914101@qq.com" }
]
keywords = [
    "rate-limiter",
    "circuit-breaker",
    "retry",
    "bulkhead",
    "resilience",
    "llm",
    "token-bucket",
    "sliding-window",
    "asyncio"
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: System :: Networking",
    "Typing :: Typed",
]
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.0.0",
    "ruff>=0.1.0",
    "mypy>=1.0.0",
]
contracts = [
    "openai>=1.0.0",
    "anthropic>=0.18.0",
    "google-genai>=0.1.0",
    "fastapi>=0.100.0",
    "httpx>=0.24.0",
]
all = [
    "openai>=1.0.0",
    "anthropic>=0.18.0",
    "google-genai>=0.1.0",
    "fastapi>=0.100.0",
    "httpx>=0.24.0",
]

[project.scripts]
flowguard = "flowguard.cli:main"

[project.urls]
Homepage = "https://github.com/yangjinyu050618-hash/flowguard"
Repository = "https://github.com/yangjinyu050618-hash/flowguard.git"
Issues = "https://github.com/yangjinyu050618-hash/flowguard/issues"
Changelog = "https://github.com/yangjinyu050618-hash/flowguard/blob/main/CHANGELOG.md"

[tool.hatch.build.targets.wheel]
packages = ["src/flowguard"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
python_files = ["test_*.py"]

[tool.ruff]
line-length = 100
target-version = "py39"

[tool.ruff.lint]
select = ["E", "F", "W"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.10"
strict = true
warn_return_any = true
warn_unused_configs = true
